═══════════════════════════════════════════════════════════════════════════════
🏭 AFFILIFY TIKTOK FACTORY - COMMAND CHEAT SHEET
═══════════════════════════════════════════════════════════════════════════════

📁 STEP 1: UPLOAD VIDEOS (2 min)
───────────────────────────────────────────────────────────────────────────────
cp /path/to/your/45/videos/*.mp4 data/raw_videos/


⚙️ STEP 2: PROCESS VIDEOS → 90 CLIPS (9 hours - run overnight!)
───────────────────────────────────────────────────────────────────────────────
python3 batch_process_videos.py \
  --input-dir data/raw_videos \
  --output-dir data/batch_output \
  --batch-size 5 \
  --break-minutes 60


🎵 STEP 3: VIEW MUSIC RECOMMENDATIONS
───────────────────────────────────────────────────────────────────────────────
cat data/batch_output/music_reports/*.txt


📥 STEP 4: DOWNLOAD MUSIC (30 min - MANUAL)
───────────────────────────────────────────────────────────────────────────────
# Download from: https://pixabay.com/music/
# Save to: data/music/*.mp3
# Name files to match clips OR use same music for all:

mkdir -p data/music
cd data/music
# (Download ONE track as background_music.mp3)
for clip in ../batch_output/split/*.mp4; do
  clip_name=$(basename "$clip" .mp4)
  cp background_music.mp3 "${clip_name}.mp3"
done
cd ../..


🎶 STEP 5: ADD MUSIC TO CLIPS (45 min)
───────────────────────────────────────────────────────────────────────────────
python3 add_music_to_clips.py \
  --clips-dir data/batch_output/split \
  --music-dir data/music \
  --output-dir data/final_clips \
  --volume 0.3


🚀 STEP 6A: TEST POSTING (1 hour - 5 accounts)
───────────────────────────────────────────────────────────────────────────────
python3 pillar5_distribution/posting_scheduler.py \
  --video-dir data/final_clips \
  --caption-dir data/batch_output/captions \
  --accounts 5 \
  --posts-per-account 2 \
  --delay-minutes 5


🔥 STEP 6B: FULL DEPLOYMENT (48 hours - ALL 60 accounts!)
───────────────────────────────────────────────────────────────────────────────
python3 pillar5_distribution/posting_scheduler.py \
  --video-dir data/final_clips \
  --caption-dir data/batch_output/captions \
  --accounts 60 \
  --posts-per-account 90 \
  --delay-minutes 10


📊 ANALYTICS: VIEW PERFORMANCE
───────────────────────────────────────────────────────────────────────────────
python3 pillar6_analytics/daily_analytics.py --date today


═══════════════════════════════════════════════════════════════════════════════
💰 EXPECTED RESULTS
═══════════════════════════════════════════════════════════════════════════════

Week 1:   5,400 posts  →  1.5M views  →  $4,500-$13,500
Month 1:  5,400 posts  →  5M views    →  $15,000-$45,000
Month 3:  5,400 posts  →  15M views   →  $45,000-$135,000

═══════════════════════════════════════════════════════════════════════════════
🎯 QUICK TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════════════════

No profiles?  → python3 pillar1_infrastructure/manual_profile_loader.py --csv data/profile_mapping.csv
MultiLogin?   → Open MultiLogin app and keep it running
No music?     → Files must match: clip1.mp4 needs clip1.mp3
Login?        → Manually login once, state is saved
Banned?       → Wait 24h, use --delay-minutes 30

═══════════════════════════════════════════════════════════════════════════════
🔥 THE FACTORY IS READY - LET'S DOMINATE TIKTOK! 🔥
═══════════════════════════════════════════════════════════════════════════════
